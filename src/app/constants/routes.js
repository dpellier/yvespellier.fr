
const BASE_ROUTE = Object.freeze({
    collages: '/collages',
    drawings: '/drawings',
    photos: '/photos',
    root: '/'
});

const BASE_ROUTES = Object.freeze(Object.values(BASE_ROUTE));

export { BASE_ROUTE, BASE_ROUTES };
